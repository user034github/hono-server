import React from 'react';
import Layout from './Layout';
import ProjectPage from './ProjectPage';

const App: React.FC = () => {
    return (
        <Layout>
            <ProjectPage />
        </Layout>
    );
};

export default App;